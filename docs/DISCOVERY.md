# Katmer Discovery

Katmer is a native C/C++ layered execution engine with a stable C ABI.

Search phrases this project is built to answer:

- C ABI execution engine
- C++ native runtime layer
- Python native worker runtime
- JavaScript native bridge
- Kotlin JNI native runtime
- arena allocator C++
- JSON trace runtime
- HTTP and DSP payload shaping engine

Related projects:

- QuickAPI: Python API runtime
- SQLNocturne: Python data runtime
